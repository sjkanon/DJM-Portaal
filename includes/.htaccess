# Deze PHP-bestanden horen alleen via require geladen te worden, nooit
# rechtstreeks opgevraagd.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>

Options -Indexes
